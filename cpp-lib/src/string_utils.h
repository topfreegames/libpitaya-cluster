#ifndef PITAYA_CLUSTER_STRING_UTILS_H
#define PITAYA_CLUSTER_STRING_UTILS_H

#include <vector>
#include <string>

namespace string_utils {

    std::vector<std::string> Split(const std::string &str, char delim);

} // namespace utils

#endif // PITAYA_CLUSTER_STRING_UTILS_H
